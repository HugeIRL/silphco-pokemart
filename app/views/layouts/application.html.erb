<!DOCTYPE html>
<html>
  <head>
    <title>FinalProject</title>
    <meta name="viewport" content="width=device-width,initial-scale=1">
    <%= csrf_meta_tags %>
    <%= csp_meta_tag %>

    <%= stylesheet_link_tag 'application', media: 'all', 'data-turbolinks-track': 'reload' %>
    <%= javascript_pack_tag 'application', 'data-turbolinks-track': 'reload' %>
  </head>

  <body>
    <nav class="navbar navbar-expand-lg navbar-light bg-white">
      <div class="container-fluid">
        <%= link_to image_tag("silph-logo.png"), root_path, class: "me-2" %>
        <%= link_to "Silph Co.", root_path, class: "text-decoration-none text-reset navbar-brand mb-0 h1" %>
        <button class="navbar-toggler" type="button" data-bs-toggle="collapse" data-bs-target="#navbarSupportedContent" aria-controls="navbarSupportedContent" aria-expanded="false" aria-label="Toggle navigation">
          <span class="navbar-toggler-icon"></span>
        </button>
        <div class="collapse navbar-collapse" id="navbarSupportedContent">
          <div class="input-group p-3">
            <input class="form-control" type="search" placeholder="Find a Pokemon" aria-label="Search">
            <div class="input-group-append">
              <button class="btn btn-primary d-flex align-items-center" type="submit">
              <svg xmlns="http://www.w3.org/2000/svg" width="16" height="16" fill="currentColor" class="bi bi-search me-2" viewBox="0 0 16 16">
                <path d="M11.742 10.344a6.5 6.5 0 1 0-1.397 1.398h-.001c.03.04.062.078.098.115l3.85 3.85a1 1 0 0 0 1.415-1.414l-3.85-3.85a1.007 1.007 0 0 0-.115-.1zM12 6.5a5.5 5.5 0 1 1-11 0 5.5 5.5 0 0 1 11 0z"/>
              </svg>
              Search
              </button>
            </div>
          </div>
          <ul class="navbar-nav mb-2 mb-lg-0">
          <% if user_signed_in? %>
            <li class="nav-item">
              Hello, <strong><%= current_user.first_name %></strong>
            </li>
          <% else %>
            <li class="nav-item">
              <%= link_to "Login", new_user_session_path, class: "nav-link text-primary px-3" %>
            </li>
            <li class="nav-item">
              <%= link_to "Register", new_user_registration_path, class: "nav-link px-3" %>
            </li>
          <% end %>
            <li class="nav-item">
              <a class="nav-link px-3" aria-current="page" href="/creatures">Shop</a>
            </li>
            <li class="nav-item dropdown">
              <a class="nav-link <% if cart.present? %> dropdown-toggle <% end %> d-flex align-items-center px-3" href="#" id="navbarDropdown" role="button" data-bs-toggle="dropdown" aria-expanded="false">
                Cart
                <% if cart.present? %>
                  <span class="badge bg-danger ms-2 me-1"><%= cart.size %></span>
                <% else %>
                  <span class="badge bg-secondary ms-2 me-1">0</span>
                <% end %>
              </a>
              <% if cart.present? %>
                <ul class="dropdown-menu dropdown-menu-end p-0" aria-labelledby="navbarDropdown">
                  <% cart.each do |creature| %>
                    <li class="dropdown-item d-flex align-items-center justify-content-between">
                      <%= link_to creature.species.titleize, creature %> <%= button_to '❌', cart_path(creature), class: "btn", method: :delete %>
                    </li>
                  <% end %>
                </ul>
              <% end %>
            </li>
          </ul>
        </div>
      </div>
    </nav>
    <div class="container">
      <div class="row">
        <% if notice %>
          <p class="alert alert-success"><%= notice %></p>
        <% end %>
        <% if alert %>
          <p class="alert alert-danger"><%= alert %></p>
        <% end %>
      </div>
      <div class="row d-flex justify-content-around">
        <%= yield %>
      </div>
    </div>
  </body>
</html>
